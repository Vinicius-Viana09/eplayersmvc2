@model Equipe
@{
    ViewData["Title"] = "Equipes";
}

<div class="titulo_pagina">
    <h1>Cadastro de @ViewData["Title"]</h1>
</div>

@* <form class="cadastro" method="POST" action='@Url.Action("Cadastrar")' enctype="multipart/form-data">
    <div class="campo">
        <label>ID</label>
        <input type="text" name="IdEquipe" />
    </div>

    <div class="campo">
        <label>Nome</label>
        <input type="text" name="Nome" />
    </div>

    <div class="campo">
        <label>Imagem</label>
        <input type="file" name="Imagem" />
    </div>

    <button class="btn gradient" type="submit">Cadastrar</button>
</form>

<table class="table table-striped table-responsive">
    <thead>
        <th class="titulo_table">ID</th>
        <th class="titulo_table">Nome</th>
        <th class="titulo_table">Foto</th>
    </thead>
    <tbody>
        @foreach (Equipe e in ViewBag.Equipes)
        {
            <tr>
                <td>@e.IdEquipe</td>
                <td>@e.Nome</td>
                <td class="img_table"><img src="/img/Equipes/@e.Imagem" alt="Imagem da equipe @e.Nome" width="50px"></td>
                <td><a asp-area="Link para excluir a equipe" asp-controller="Equipe" asp-action="Excluir"
                    asp-route-id="@e.IdEquipe">Excluir</a></td>
            </tr>
        }
    </tbody>
</table> *@

<form class="box_cadastro" method="POST" action='@Url.Action("Cadastrar")' enctype="multipart/form-data">
    <div>
        <label class="titulo_input">ID</label>
        <input type="text" name="IdEquipe" />
        <label class="titulo_input">Nome da Equipe</label>
        <input name="IdEquipe" class="input_grande" type="Text">
        <label class="titulo_input">Emblema</label>
        <input type="file" name="Imagem" />
    </div>
    <button class="botão_1" type="submit">Cadastrar</button>
</form>
<h1>Lista de Times</h1>
<div class="box_exemplo">
    <table class="table table-striped table-responsive">
    <thead>
        <th class="titulo_table">ID</th>
        <th class="titulo_table">Nome</th>
        <th class="titulo_table">Foto</th>
    </thead>
    <tbody>
        @foreach (Equipe e in ViewBag.Equipes)
        {
            <tr>
                <td>@e.IdEquipe</td>
                <td>@e.Nome</td>
                <td class="img_table"><img src="/img/Equipes/@e.Imagem" alt="Imagem da equipe @e.Nome" width="50px"></td>
                <td><a asp-area="Link para excluir a equipe" asp-controller="Equipe" asp-action="Excluir"
                    asp-route-id="@e.IdEquipe">Excluir</a></td>
            </tr>
        }
    </tbody>
</table>
</div>